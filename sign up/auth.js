(function () { const sessionKey = 'nirikshanDemoSession'; if (localStorage.getItem(sessionKey) !== 'active') { location.replace('../login/'); return; } document.addEventListener('DOMContentLoaded', () => { const logout = document.getElementById('logout-button'); if (logout) logout.addEventListener('click', () => { localStorage.removeItem(sessionKey); location.replace('../login/'); }); }); })();
